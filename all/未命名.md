```mermaid
graph TB
    %% 定义上层四个节点（横向排列，统一样式）
    A[CAD]:::upperNode --- B[CAE]:::upperNode --- C[CAPP]:::upperNode --- D[CAM]:::upperNode
    
    %% 定义下层核心节点
    E[CAX 技术体系]:::coreNode
    
    %% 连线：还原\、|、|、/的视觉效果
    A --\--> E
    B --|--> E
    C --|--> E
    D --/--> E
    
    %% 样式定义（可选，优化视觉，iodraw支持）
    classDef upperNode fill:#f0f8ff,stroke:#2f4f4f,stroke-width:1,rounded:5,font-size:14
    classDef coreNode fill:#e6f7ff,stroke:#1890ff,stroke-width:2,rounded:8,font-size:16,font-weight:bold
    
    %% 布局优化：让上层节点与核心节点居中对齐（iodraw自动识别）
    linkStyle 0,1,2 gap:20; % 调整上层节点间距
    linkStyle 3,4,5,6 gap:30; % 调整上下层连线间距
```